1. vo.Video、vo.Chapter、vo.Course增加search、json和release方法，使用检索方法和释放资源更佳的简洁
`原先`
```python
from vsearcher import VSearcher
executed_video = VSearcher.executeVideo(video_file_path='./test.mp4')  # 对视频进行处理, 并返回处理结果
search_result = VSearcher.search(executed_video, key="学习") # 搜索并返回结果
VSearcher.releaseByOutputDir(video.output_dir) # 释放资源
# 无法直接print查看search_result 和 executed_video
```

`改动后（简洁、见名知意)`
```python
from vsearcher import VSearcher
executed_video = VSearcher.executeVideo(video_file_path='./test.mp4' )  # 对视频进行处理, 并返回处理结果
search_result = executed_video.search(key= "学习")  # 返回搜索结果
executed_video.release()  # 释放资源

# 可进行序列化打印查看
print(executed_video.json().encode('utf8').decode('unicode-escape'))
print(search_result.json().encode('utf8').decode('unicode-escape'))
```
2. utils.local2url() 和 utils.url2local, 传入非法路径，直接返回None

3. vo.Video 新增 cw_local 属性， 即课件的本地地址

4. 进一步完善和规范注释

5. VSearcher.executeChapter() and VSearcher.executeCourse() 新增name属性