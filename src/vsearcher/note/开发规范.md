1. 目录路径规范: D:/a/b  也就是结尾不加/or\\，即 D:/a/b/  (x)
2. 创造功能函数, 参数需要传入文件名时, 传入的默认设置为不需要传入文件类型后缀
```python
def get_model_by_name(model_name) -> model:
    # model_name = model_name (√)
    # model_name = model_name.pkl (×)
    # return model
```
3. 函数参数默认值为None：VSearcher自动进行取值
```python
def executeVideo(video_path, video_name=None):
    """ 此处的video_name=None时，代表自动根据路径获得视频文件名 """
    video_output
    return video_output
```